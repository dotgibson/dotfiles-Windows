name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Cancel superseded runs on the same ref so the queue stays current.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Pinned dependency versions live in one place so a bump is a one-line, reviewable
# change (and Renovate can PR action SHAs -- see renovate.json). Actions are pinned
# SHA — a moved tag can't silently change what runs in CI (supply-chain gate).
env:
  PESTER_VERSION: "5.6.1"
  PSSA_VERSION: "1.22.0"
  LUACHECK_VERSION: "1.2.0"

permissions:
  contents: read

jobs:
  # --- incremental gate -------------------------------------------------------
  # Decide whether the heavy Windows jobs need to run at all. A docs/markdown-only
  # change still gets the fast Linux gate, but doesn't spin up two windows-latest
  # runners for nothing. `code` is true when any changed file is NOT pure docs.
  detect-changes:
    name: Detect code changes
    runs-on: ubuntu-latest
    outputs:
      code: ${{ steps.filter.outputs.code }}
      lua: ${{ steps.filter.outputs.lua }}
      starship: ${{ steps.filter.outputs.starship }}
      theme: ${{ steps.filter.outputs.theme }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - id: filter
        shell: bash
        run: |
          set -euo pipefail
          ZERO='0000000000000000000000000000000000000000'
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base='${{ github.event.pull_request.base.sha }}'
          else
            base='${{ github.event.before }}'
          fi
          # An empty / all-zero (new branch) / unreachable base can't anchor a diff.
          # Fall back to HEAD~1; if even that is missing (root commit, shallow clone),
          # we have no usable base.
          if [ -z "$base" ] || [ "$base" = "$ZERO" ] || ! git cat-file -e "${base}^{commit}" 2>/dev/null; then
            if git rev-parse HEAD~1 >/dev/null 2>&1; then base='HEAD~1'; else base=''; fi
          fi
          # FAIL SAFE: when the base is unknown, run EVERYTHING rather than skipping
          # the heavy jobs on a guess — the old HEAD~1 fallback could yield an empty
          # diff and silently skip tests on a root commit / new branch / force-push.
          if [ -z "$base" ]; then
            echo "no usable diff base (root commit / new branch) — running all jobs"
            echo 'code=true' >> "$GITHUB_OUTPUT"
            echo 'lua=true'  >> "$GITHUB_OUTPUT"
            echo 'starship=true' >> "$GITHUB_OUTPUT"
            echo 'theme=true' >> "$GITHUB_OUTPUT"
            exit 0
          fi
          changed="$(git diff --name-only "${base}...HEAD")"
          echo "diff base: $base"; echo "changed files:"; echo "$changed"
          # code=true if at least one changed path is NOT a docs/markdown file
          # (anything under docs/ or any *.md is treated as docs-only).
          if printf '%s\n' "$changed" | grep -qvE '^(docs/.*|.*\.md)$'; then
            echo 'code=true' >> "$GITHUB_OUTPUT"
          else
            echo 'code=false' >> "$GITHUB_OUTPUT"
          fi
          # lua=true when any Neovim Lua config (or its luacheck config) changed,
          # so the Lua lint only spins up when it can actually find something.
          # nvim.lock is in the set even though it holds no Lua: it is the vendored
          # tree's pin, and the `nvim Core parity` job below re-clones at the commit it
          # records. Leave it out and a PIN-ONLY bump — exactly what the nvim-sync bot
          # opens when upstream cuts a release with no file changes — skips the one gate
          # that could catch it pointing somewhere wrong.
          if printf '%s\n' "$changed" | grep -qE '(^nvim/|^nvim\.lock$|\.lua$|\.luacheckrc$)'; then
            echo 'lua=true' >> "$GITHUB_OUTPUT"
          else
            echo 'lua=false' >> "$GITHUB_OUTPUT"
          fi
          # starship=true when the mirrored starship.toml or its provenance marker
          # changed — the parity gate only has something to say about those two.
          if printf '%s\n' "$changed" | grep -qE '^starship/'; then
            echo 'starship=true' >> "$GITHUB_OUTPUT"
          else
            echo 'starship=false' >> "$GITHUB_OUTPUT"
          fi
          # theme=true when the vendored palette or its provenance marker changed -
          # that is all the PARITY gate can speak to. The GENERATION gate below is
          # deliberately not filtered on this: a hand-edit to a rendered block in
          # 10-tools.ps1 or psmux.conf never touches theme/, and that is precisely the
          # edit it exists to catch, so it runs under `code` with the rest of the suite.
          if printf '%s\n' "$changed" | grep -qE '^theme/'; then
            echo 'theme=true' >> "$GITHUB_OUTPUT"
          else
            echo 'theme=false' >> "$GITHUB_OUTPUT"
          fi

  # --- fast, dependency-free gate ---------------------------------------------
  # Runs on Linux pwsh in seconds and fails the build on any syntax/JSON/format
  # regression before the heavier Windows jobs spin up. Always runs.
  lint-fast:
    name: Syntax, manifest & format gate (fast)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Validate (parser + JSON + editorconfig, no Gallery needed)
        shell: pwsh
        run: ./tests/Invoke-Validation.ps1

  # --- Markdown lint ----------------------------------------------------------
  # .markdownlint.jsonc has existed since the README rewrite but NOTHING ran it —
  # the file's own header said so. A config nothing enforces is worse than none,
  # because it reads as a guarantee. Kept out of lint-fast so that job stays
  # dependency-free (no npm); scoped to README.md, matching the config's `files`.
  markdown-lint:
    name: Markdownlint (README)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Lint README against .markdownlint.jsonc
        shell: bash
        # Pinned like every other tool version here: a floating @latest could fail
        # the build on a rule that did not exist when the README was written.
        run: npx --yes markdownlint-cli@0.45.0 --config .markdownlint.jsonc README.md

  # --- Lua lint (nvim config) -------------------------------------------------
  # The nvim/ tree had a .luacheckrc but nothing ran it, so the config could rot
  # silently. Runs only when Lua actually changed (see detect-changes.lua).
  lua-lint:
    name: Luacheck (nvim)
    runs-on: ubuntu-latest
    needs: [lint-fast, detect-changes]
    if: needs.detect-changes.outputs.lua == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Install lua + luarocks
        shell: bash
        run: |
          set -euo pipefail
          # lua5.1 + luarocks provide the runtime luacheck needs to execute, so
          # they come from apt every run (the cached rock below can't run without
          # them). The slow, non-hermetic part — compiling the luacheck rock — is
          # what we pin and cache.
          sudo apt-get update
          # liblua5.1-0-dev so luarocks can compile luacheck's C deps (luafilesystem)
          # against the SAME Lua 5.1 we standardize on below.
          sudo apt-get install -y lua5.1 liblua5.1-0-dev luarocks
      - name: Cache luacheck rock
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.luarocks
          # Key on the runner image (ImageOS) and the Lua line too: the cached tree
          # is built against the runner's lua5.1/luarocks, so a runner-image bump or
          # Lua change must invalidate it rather than silently reuse a mismatched rock.
          key: luacheck-${{ runner.os }}-${{ env.ImageOS }}-lua5.1-${{ env.LUACHECK_VERSION }}
      - name: Install luacheck (pinned)
        shell: bash
        run: |
          set -euo pipefail
          # Pin the exact version (matches the SHA-pinned Actions discipline) AND the
          # Lua line (--lua-version 5.1) so the build target is deterministic instead
          # of luarocks' default; install to the cached user tree, so a cache hit
          # skips the recompile.
          if ! "$HOME/.luarocks/bin/luacheck" --version 2>/dev/null | grep -q "$LUACHECK_VERSION"; then
            luarocks install --local --lua-version 5.1 luacheck "$LUACHECK_VERSION"
          fi
          echo "$HOME/.luarocks/bin" >> "$GITHUB_PATH"
      - name: Luacheck
        shell: bash
        # Run from nvim/ so luacheck discovers nvim/.luacheckrc by searching upward.
        run: luacheck .
        working-directory: nvim

  # --- nvim <-> upstream parity (B1) ------------------------------------------
  # nvim/ is vendored from dotgibson/dotfiles-nvim; this fails if the vendored tree
  # drifts from the commit recorded in nvim.lock (clone that repo @ that commit and
  # diff). Self-skips until a sync stamps the lock. Same nvim-only gate as luacheck.
  #
  # NB: job `name:` values are ASCII on purpose. The check-run name is what you type
  # into a required-status-check ruleset, and a unicode arrow there is a copy-paste
  # trap that silently never matches.
  #
  # `nvim Core parity` IS DELIBERATELY UNCHANGED, though the editor no longer comes
  # from Core (dotfiles-core#1124). The string is one of the seven contexts the main
  # ruleset requires: rename the job and the old context can never arrive again, so
  # every PR sits BLOCKED until someone edits branch protection to match. Renaming it
  # is a two-step change — ruleset first, job second — and worth doing on its own,
  # not smuggled into a PR whose merge depends on it.
  nvim-parity:
    name: nvim Core parity
    runs-on: ubuntu-latest
    needs: [lint-fast, detect-changes]
    if: needs.detect-changes.outputs.lua == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Assert nvim/ matches dotfiles-nvim at the recorded ref
        shell: pwsh
        run: ./tests/Assert-NvimParity.ps1

  # The sibling gate for the other mirrored asset. starship/starship.toml had no
  # parity check at all and had already drifted once (ten non-sync commits, later
  # reconciled by 9deb31a); this is what stops a repeat.
  starship-parity:
    name: starship Core parity
    runs-on: ubuntu-latest
    needs: [lint-fast, detect-changes]
    if: needs.detect-changes.outputs.starship == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Assert starship.toml matches Core at the recorded ref
        shell: pwsh
        run: ./tests/Assert-StarshipParity.ps1

  # The third mirrored asset. theme/palette.toml is the INPUT gen-theme.ps1 renders
  # the whole terminal layer from, so a local edit here would not fork one config —
  # it would fan a half-recoloured stack out through a generator that reports success.
  theme-parity:
    name: theme Core parity
    runs-on: ubuntu-latest
    needs: [lint-fast, detect-changes]
    if: needs.detect-changes.outputs.theme == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Assert palette.toml matches Core at the recorded ref
        shell: pwsh
        run: ./tests/Assert-ThemeParity.ps1

  # The generation gate — the half that catches a hand-edit to a rendered block.
  # Runs on every code change, not just theme/ ones: the drift this closes (#228)
  # lived in powershell/core/10-tools.ps1, and a filter on theme/ would have missed
  # it exactly as the old hand-maintained comment did.
  theme-gen:
    name: theme generation up to date
    runs-on: ubuntu-latest
    needs: [lint-fast, detect-changes]
    if: needs.detect-changes.outputs.code == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Assert every generated block matches theme/palette.toml
        shell: pwsh
        run: ./gen-theme.ps1 -Check

  analyze:
    name: PSScriptAnalyzer
    runs-on: windows-latest
    needs: [lint-fast, detect-changes]
    if: needs.detect-changes.outputs.code == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache PowerShell modules
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/Documents/PowerShell/Modules
          key: pssa-${{ runner.os }}-${{ env.PSSA_VERSION }}
      - name: Install PSScriptAnalyzer (pinned)
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable PSScriptAnalyzer |
                    Where-Object Version -eq ([version]$env:PSSA_VERSION))) {
            Set-PSRepository PSGallery -InstallationPolicy Trusted
            Install-Module PSScriptAnalyzer -RequiredVersion $env:PSSA_VERSION `
              -Scope CurrentUser -Force
          }
      # Supply-chain gate: -RequiredVersion above pins the version and Install-Module
      # enforces the publisher check, but nothing asserted the installed bits are
      # actually Microsoft-signed (a poisoned cache or a compromised feed would slip
      # through). Verify the module manifest's Authenticode signature is Valid AND
      # Microsoft-issued before trusting the analyzer — the Windows analogue of the
      # SHA-256 verification the Linux gate tools get from setup-core-tools.
      - name: Verify PSScriptAnalyzer is Microsoft-signed
        shell: pwsh
        run: |
          $mod = Get-Module -ListAvailable PSScriptAnalyzer |
            Where-Object Version -eq ([version]$env:PSSA_VERSION) |
            Select-Object -First 1
          if (-not $mod) { throw "PSScriptAnalyzer $($env:PSSA_VERSION) not installed" }
          $sig = Get-AuthenticodeSignature -FilePath $mod.Path
          "status=$($sig.Status)" | Write-Host
          # Check Status FIRST: on a non-Valid status (e.g. NotSigned) SignerCertificate
          # is $null, so reading .Subject before this would mask the real failure.
          if ($sig.Status -ne 'Valid') {
            throw "PSScriptAnalyzer signature is '$($sig.Status)', expected 'Valid'"
          }
          $subject = $sig.SignerCertificate.Subject
          "signer=$subject" | Write-Host
          if ($subject -notmatch 'Microsoft Corporation') {
            throw "PSScriptAnalyzer signer '$subject' is not Microsoft Corporation"
          }
          "PSScriptAnalyzer $($env:PSSA_VERSION) verified: Valid + Microsoft-signed" | Write-Host
      - name: Analyze
        shell: pwsh
        run: |
          Import-Module PSScriptAnalyzer -RequiredVersion $env:PSSA_VERSION
          $results = Invoke-ScriptAnalyzer -Path . -Recurse `
              -Settings ./tests/PSScriptAnalyzerSettings.psd1
          if ($results) {
              $results | Format-Table -AutoSize | Out-String | Write-Host
          }
          $errors = @($results | Where-Object Severity -eq 'Error')
          "Analyzer findings: $($results.Count) total, $($errors.Count) error(s)" | Write-Host
          if ($errors.Count) { throw "PSScriptAnalyzer reported $($errors.Count) error(s)" }

  test:
    name: Pester
    runs-on: windows-latest
    needs: [lint-fast, detect-changes]
    if: needs.detect-changes.outputs.code == 'true'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache PowerShell modules
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/Documents/PowerShell/Modules
          key: pester-${{ runner.os }}-${{ env.PESTER_VERSION }}
      - name: Install Pester (pinned)
        shell: pwsh
        run: |
          if (-not (Get-Module -ListAvailable Pester |
                    Where-Object Version -eq ([version]$env:PESTER_VERSION))) {
            Set-PSRepository PSGallery -InstallationPolicy Trusted
            Install-Module Pester -RequiredVersion $env:PESTER_VERSION `
              -Scope CurrentUser -Force -SkipPublisherCheck
          }
      - name: Run tests (with coverage gate)
        shell: pwsh
        # Single source of truth: the same command a contributor runs locally.
        # The gate (coverage bar, exact test-file match, test-case floor) lives in
        # tests/Invoke-Tests.ps1 so "passes locally" and "passes CI" are the same
        # assertion rather than two that can drift apart unnoticed.
        run: ./tests/Invoke-Tests.ps1
